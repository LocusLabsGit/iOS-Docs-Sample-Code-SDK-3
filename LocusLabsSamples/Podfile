# Uncomment the next line to define a global platform for your project
platform :ios, '10.0'

target 'LocusLabsSamples' do
  
  use_frameworks!

  # Pods for LocusLabsSamples
  pod 'LocusLabs-iOS-SDK', '3.1.0'
  
  # Only needed if integrating GRAB
  pod 'CardIO'
end
